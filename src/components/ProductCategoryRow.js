import React from "react";

const ProductCategoryRow = (props) => {
  return (
    <tr>
      <th>
        {/* <span>{this.props.products}</span> */}
      </th>
    </tr>
  );
};

export default ProductCategoryRow;
